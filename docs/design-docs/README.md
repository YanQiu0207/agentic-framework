# `docs/design-docs/` 已停止写入

**状态**：Superseded

当前项目的 Change Artifact 已统一到 [`openspec/changes/`](../../openspec/changes/)，长期辅助知识位于 [`openspec/specs/`](../../openspec/specs/)。

本目录仅保留迁移前的历史来源和兼容引用。新 Workflow 禁止向此处写入；未经用户明确授权，不得删除旧文件。
