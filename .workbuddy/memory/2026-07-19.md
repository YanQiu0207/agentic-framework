# 2026-07-19 工作日志

## 审核 add-bp-cli-tool-design 变更实现

- **对象**：`openspec/changes/archive/2026-07-19-add-bp-cli-tool-design`（已归档变更的事后独立审核）
- **diff 基线**：`0c26b76d22145fed04e517903e52c3060411f6b2` → `512ead6`
- **总评**：PASS（P0=0, P1=0, P2=2 项非阻塞建议）
- **审核报告**：`openspec/changes/archive/2026-07-19-add-bp-cli-tool-design/audit-report.md`

### 审核发现

1. **Task 1 (bp-cli-tool-design Skill)**：迁移质量高于来源——「敏感信息与日志」一节明确封死通过 `str(exc)` / `exc.args` / 异常链 / traceback 间接泄漏敏感信息的路径，是相对于来源 Skill 的安全增强。
2. **Task 2/3 (双 Profile 接入 + 安装器)**：实现与 `tasks.md` 描述完全吻合，`CORE_SKILLS` 与 `MANAGED_PROFILE_SKILL_ROOTS` 两个集合都更新（对应风险 #2 处理）。
3. **Task 4 (统一验证)**：`.verify/report.json` 和 `.verify/review-report.json` 都是 PASS，Run 级 Review 是第 2 轮复审通过（F-1 参考文档精简、F-2 异常脱敏均已修复）。

### 环境差异说明

- 在当前 WorkBuddy 桌面环境（Windows 非特权账户）下，`scripts/test_install_agentic_framework.py` 30 个测试全部在 `setUp` 阶段失败——根因是 `probe_target.symlink_to(probe_source)` 未抛 `OSError` 但实际未创建链接，导致 `probe_target.unlink()` 抛 `FileNotFoundError`。
- 通过 `git stash` 在干净工作区复现，确认是环境问题（Windows 符号链接权限），与本次变更无关。
- 用直接调用 `installer.build_operations()` 绕过文件系统层验证，确认两个 Profile 的 `.codex` / `.claude` 目标都包含 `bp-cli-tool-design`。
- `lint_skill_graph.py` 和 `test_profile_contracts.py` 在当前环境都通过。

### P2 建议

- P2-1：proposal.md / tasks.md 中来源路径写的是 `references/`（复数），本仓库实际是 `reference/`（单数，符合 `bp-skill-authoring` 规范）。仅表述不一致，不影响实现。
- P2-2：建议 `test_install_agentic_framework.py::setUp` 在 `symlink_to` 后用 `lstat` 验证链接确实存在，否则 `skipTest`，让逻辑层断言可在 Windows 非特权环境下复现。

### 经验

- WorkBuddy 桌面环境的 `sitecustomize.py`（路径 `C:/Users/YanQi/AppData/Local/Programs/WorkBuddy/resources/app.asar.unpacked/cli/vendor/shim/sitecustomize.py`）会注入 `_safe_path_unlink` shim，对 Path.unlink 拦截。清空 `CODEBUDDY_SESSION_ID` / `CLAUDE_SESSION_ID` 后 shim 不再激活，但 Windows 非特权账户本身的符号链接限制依然存在。
- 在本仓库审核涉及符号链接的测试时，可直接调用被测函数（如 `build_operations`）绕过文件系统层验证逻辑正确性。

## 预审核 machine-verifiable-agent-runtime change（文档阶段）

- **对象**：`openspec/changes/machine-verifiable-agent-runtime`（proposal/design/spec/tasks 四件套，状态 Proposed，未实现）
- **总体结论**：合理，建议批准进入实现。四份文档内部一致，且核心缺口主张经代码核实属实（verify.py 报告确无 run_id/task_id/commit 绑定；workflow_control.py 有 task_id/attempts 但无 run 级身份）。
- **可追源性**：6 个 spec ADDED Requirement ↔ design §3/§4 六节 ↔ tasks Task2~7 一一对应，无孤儿设计、无遗漏需求。
- **已核实事实**：workflow_control.py、verify.py、4 个 `evaluation/trigger-cases.md`（code-generation/code-review/requirements-clarification/verification）、ADR 003、docs/tooling/08、13 均存在；`review_profile`(lightweight/standard/strict) 是框架既有任务约定。
- **需补强点（已反馈用户）**：① `.agentic-framework/runs/` 未被 `.gitignore` 忽略（当前仅忽略 `.verify/`），运行产物有被误提交风险；② `schemas/runtime/` 为新顶层目录，需明确落盘位置；③ Envelope `config_digest` 的「config」来源未定义、`attempt` 与 workflow_control 既有 `attempts` 计数语义需对齐；④ spec 中「核心 Agent 行为可回归」只点名部分用例族，`conflicting-instructions` 未作为独立需求显式覆盖。
