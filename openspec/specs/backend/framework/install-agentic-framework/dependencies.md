# 框架安装器依赖

- Python 标准库：`argparse`、`hashlib`、`json`、`pathlib`、`shutil`、`tempfile` 等。
- 框架源资产：`skills/`、`commands/`、`agents/` 和根 `scripts/`。
- 目标侧状态：`.agentic-framework/manifest.json`。
- 用户侧状态：`~/.agentic-framework/installations.json`。

安装器不依赖第三方 Python 包。
